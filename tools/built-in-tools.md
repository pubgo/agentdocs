# Built-in Tools

I have the following built-in tools available:

## File Operations

- `read_file` — Read file contents
- `write_file` — Write content to a file
- `edit` — Replace text within a file
- `list_directory` — List files/folders in a directory
- `glob` — Find files by name pattern (e.g., `**/*.ts`)

## Search

- `grep_search` — Search file contents using regex
- `agent` — Launch specialized sub-agents for complex tasks (e.g., Explore, general-purpose)

## Shell

- `run_shell_command` — Execute terminal commands (`git`, `npm`, `make`, etc.)

## Web

- `web_search` — Search the web for up-to-date info
- `web_fetch` — Fetch and extract content from a URL

## Task Management

- `todo_write` — Track and manage multi-step tasks
- `ask_user_question` — Ask you questions for clarification
- `save_memory` — Remember user-specific facts/preferences

## Skills

- `review` — Review code changes for correctness, security, quality
- `qc-helper` — Answer Qwen Code usage/configuration questions
- `loop` — Schedule recurring prompts
